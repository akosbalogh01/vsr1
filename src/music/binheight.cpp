#include "consts.hpp"
#include "music.hpp"

const unsigned vs::music::getBinHeight(const unsigned bin) const {
    //return sqrt(pow(bins[bin].imag(), 2) + pow(bins[bin].real(), 2))/500;
    return 300;
}
